# PLAN: Workbench 구현·검토·검증

## 단계 1. 기존 결과물 확인

기존 ZIP의 Engine.run, Twin.from_csv, TwinStore, Request/Snapshot/Result 스키마, 예제 JSON과 4개 소비 CSV를 직접 확인한다. 이전 안내의 개념 예시 대신 실제 코드 계약을 프론트 입력의 기준으로 사용한다.

산출물: 원 엔진 코드 보존, 원 문서 `docs/engine_v0_1/` 보관, 루트 SPEC의 확장 범위 정의.

## 단계 2. 실행·저장 경계 구현

FastAPI API와 동일 출처 정적 프론트를 만든다. upload profile과 Twin 생성은 분리하여 사용자가 스냅샷을 입력할 수 있게 한다. 사용자 파일명은 저장 경로에 쓰지 않는다.

별도 spawn 수치 프로세스, 동시 작업 제한, 결과 파일 원자적 교체, 부모의 Twin 커밋을 구현한다. 취소·예외·시간 초과·서버 종료의 네 경로에서 자식을 정리한다. 저장된 Twin과 임시 작업을 다른 디렉터리에 둔다.

검토: 부분 생성 DB가 영구 목록에 남는지, 작업 ID와 경로가 검증되는지, 브라우저 새로고침 후 진행 중인 작업이 방치되는지 확인한다.

## 단계 3. 프론트 6개 페이지

CSV 드래그 업로드/파일 선택, 프로필 미리보기, 3가지 상태 입력 방식, 데모 선택, Twin 목록을 만든다. 각 모드에서 공통 및 모드별 폼과 원문 JSON 편집을 제공한다.

% UI와 0~1 JSON 비율 변환, 비어 있는 선택적 필드, 스냅샷 예비자금 상속, 원문 JSON의 스키마 검증 후 폼 전환을 검토한다. 입력 변경 시 이전 결과 표시를 숨기지 않는다.

결과 JSON·수치·시각화·가정/경고·실행 입력 탭과 JSON 저장/복사를 구현한다. 차트는 null을 0으로 바꾸지 않고 원래 데이터 필드에 연결한다. 좁은 화면에서 장문 JSON의 intrinsic width가 전체 페이지를 밀어내지 않도록 검사한다.

## 단계 4. Windows 통합 실행기

START.bat는 설치된 Python 확인 → 가상환경 생성 → 같은 창에서 launcher.py 실행 순서로 만든다. launcher.py는 의존성 준비와 서버 실행을 맡는다. 브라우저는 서버가 준비된 이후 연다.

파일 잠금으로 같은 데이터 폴더 중복 실행을 차단하고, 점유 포트를 다른 프로그램 종료 없이 처리한다. Ctrl+C를 서버 하나의 수명 관리 흐름으로 모은다. Windows 경로 인용과 CRLF, 한글/공백 경로를 고려한다.

검토: `start /b`, 별도 Node 서버, 전역 taskkill, PowerShell 정책 변경을 넣지 않는다. 실제 Windows 콘솔 이벤트는 수동 인수 체크리스트로 별도 남긴다.

## 단계 5. 자동 테스트

- 기존 엔진 회귀 테스트 160개를 그대로 실행한다.
- FastAPI 경로에서 4페르소나×5모드를 실행하고 저장된 Twin의 직접 Engine.run과 전체 결과를 비교한다.
- 정상/중복/혼합/잘못된 헤더/비UTF-8/빈 CSV/큰 본문/파일 수/경로 조작을 검증한다.
- JSON 스키마, snapshot null, 미래 snapshot, 실패 생성의 미커밋, 삭제, 세션 토큰·Host·Origin을 검증한다.
- 이력 전용 Twin의 null/insufficient_data, 사용자 이름의 HTML 문자열 처리, 진행 작업 재발견을 점검한다.
- 실제 launcher 프로세스를 띄워 유휴 Ctrl+C와 계산 중 Ctrl+C, 자식 프로세스 종료, 같은 포트 재실행, Twin 영속성, 중복 서버 차단을 검증한다.

코드: `tests/`, `tests_web/`. 실제 결과는 `qa/pytest-output.txt`, `qa/pytest.xml`, `QA_REPORT.md`에 기록한다.

## 단계 6. 브라우저 QA

정상 브라우저에서는 `scripts/ui_smoke.py`의 native HTTP 모드로 업로드·간편 상태 입력·5모드 폼·JSON 직접 편집·스키마 오류·취소·표/그래프·모바일을 확인한다.

현재 환경의 관리형 Chromium은 URL 탐색을 차단하고 테스트 브라우저 다운로드도 DNS 제한으로 실패했다. 따라서 브라우저 보안 정책을 변경하지 않고 **로컬 소스를 about:blank에 주입하고 fetch를 실제 로컬 HTTP 클라이언트에 연결하는 테스트 하네스**를 사용한다. 동일한 UI 소스와 실제 서버 결과를 점검하지만 native HTTP/CSP/OS 클립보드·다운로드까지 검증한 것으로 기록하지 않는다.

UI 검증 로그: `qa/ui-report.json`. 데스크톱/모바일 스크린샷도 보관한다. Windows 실제 브라우저 확인은 `docs/WINDOWS_CHECKLIST.md`에 남긴다.

## 단계 7. 배포 ZIP 검토

원 엔진 파일 변경 여부를 해시로 확인하고, zip을 새 경로에 풀어 import·정적 파일·API·실행기를 재검사한다. .venv, 실제 테스트 Twin, 임시 업로드, __pycache__, 테스트 중간 실패 로그는 배포하지 않는다.

패키지에는 소스, 소비001~004, 예제 JSON, START.bat, start.sh, README, SPEC, PLAN, QA 보고서와 재현 가능한 테스트를 포함한다. 파일별 SHA-256 목록을 작성한다.

## 구현 상태

단계 1~7을 수행했다. 자동 테스트 227개, UI 체크 18개, 한글·공백 경로의 압축 해제 실행 체크 13개가 통과했다. 원 엔진 파일 15개가 원본과 일치한다. 구체적인 검증 범위와 미검증 항목은 `QA_REPORT.md`를 최종 기준으로 삼는다. 네이티브 Windows 확인과 최초 인터넷 설치는 이 환경에서 실행했다고 주장하지 않는다.
